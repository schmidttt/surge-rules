name: Sync BiliBili rules

on:
  workflow_dispatch:
  schedule:
    - cron: "10 4 * * *"
      timezone: "Asia/Shanghai"
    - cron: "10 4 * * 2,4,6"
      timezone: "Asia/Shanghai"

concurrency:
  group: bilibili-rules-sync
  cancel-in-progress: false

jobs:
  build-and-open-review-pr:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (vars.ENABLE_SCHEDULED_SYNC == 'true' &&
        ((github.event.schedule == '10 4 * * *' && vars.SYNC_PHASE != 'stable') ||
         (github.event.schedule == '10 4 * * 2,4,6' && vars.SYNC_PHASE == 'stable')))
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Check out default branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"

      - name: Run offline tests
        run: |
          python3 -m unittest discover -s tests/shared -v
          python3 -m unittest discover -s tests/media -v

      - name: Resolve and pin current upstream commits
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: python3 scripts/media/build_media_rules.py --product bilibili --fetch

      - name: Normalize risk classification
        run: >-
          python3 scripts/shared/risk_policy.py
          --assessment reports/bilibili/change-assessment.json

      - name: Validate repository
        run: python3 scripts/validate_repository.py

      - name: Show generated changes
        run: git diff --check && git diff --stat

      - name: Detect changes
        id: changes
        shell: bash
        run: |
          if git diff --quiet -- rules/BiliBili reports/bilibili; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Read risk assessment
        if: steps.changes.outputs.changed == 'true'
        id: risk
        shell: bash
        run: |
          echo "auto_merge=$(jq -r '.auto_merge_eligible' reports/bilibili/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "classification=$(jq -r '.classification' reports/bilibili/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "added=$(jq -r '.added_count' reports/bilibili/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "removed=$(jq -r '.removed_count' reports/bilibili/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "ratio=$(jq -r '.actual_change_ratio' reports/bilibili/change-assessment.json)" >> "$GITHUB_OUTPUT"

      - name: Create or update review branch
        if: steps.changes.outputs.changed == 'true'
        id: review_pr
        env:
          GH_TOKEN: ${{ github.token }}
          BASE_BRANCH: ${{ github.event.repository.default_branch }}
          REVIEW_BRANCH: automation/bilibili-rules-sync
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin "$REVIEW_BRANCH:refs/remotes/origin/$REVIEW_BRANCH" || true
          git switch -C "$REVIEW_BRANCH"
          git add rules/BiliBili reports/bilibili
          git commit -m "chore: sync BiliBili rules"
          git push --force-with-lease origin "HEAD:$REVIEW_BRANCH"

          REASONS=$(jq -r 'if (.reasons | length) == 0 then "none" else (.reasons | join(", ")) end' reports/bilibili/change-assessment.json)
          cat > /tmp/bilibili-rules-pr.md <<EOF
          Automated BiliBili.list refresh.

          - Classification: ${{ steps.risk.outputs.classification }}
          - Added rules: ${{ steps.risk.outputs.added }}
          - Removed rules: ${{ steps.risk.outputs.removed }}
          - Actual change ratio: ${{ steps.risk.outputs.ratio }}
          - Review reasons: ${REASONS}

          v2fly is the only formal upstream. BlackMatrix7 and Sukka remain comparison-only.
          Detailed changes: reports/bilibili/change-assessment.json
          Review guide: docs/rules/media/REVIEW_CHECKLIST.md
          EOF

          PR_URL=$(gh pr list \
            --base "$BASE_BRANCH" \
            --head "$REVIEW_BRANCH" \
            --state open \
            --json url \
            --jq '.[0].url // ""')

          if [ -n "$PR_URL" ]; then
            gh pr edit "$PR_URL" --body-file /tmp/bilibili-rules-pr.md
          else
            PR_URL=$(gh pr create \
              --base "$BASE_BRANCH" \
              --head "$REVIEW_BRANCH" \
              --title "chore: review BiliBili rules sync" \
              --body-file /tmp/bilibili-rules-pr.md)
          fi
          echo "url=$PR_URL" >> "$GITHUB_OUTPUT"
          echo "head_sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Notify repository owner for risky refresh
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.classification != 'low-risk'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 scripts/shared/notify_review.py \
            --repository "${{ github.repository }}" \
            --pr-url "${{ steps.review_pr.outputs.url }}" \
            --reviewer "${{ github.repository_owner }}" \
            --product "BiliBili" \
            --assessment reports/bilibili/change-assessment.json \
            --evidence reports/bilibili/bilibili-report.json \
            --evidence reports/bilibili/reference-audit.json \
            --review-guide docs/rules/media/REVIEW_CHECKLIST.md

      - name: Auto-merge low-risk refresh
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.auto_merge == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 scripts/shared/merge_low_risk.py \
            --repository "${{ github.repository }}" \
            --pr-url "${{ steps.review_pr.outputs.url }}" \
            --assessment reports/bilibili/change-assessment.json \
            --workflow validate-repository.yml \
            --allowed-path rules/BiliBili \
            --allowed-path reports/bilibili \
            --expected-branch automation/bilibili-rules-sync \
            --expected-head-sha "${{ steps.review_pr.outputs.head_sha }}"

      - name: Keep risky refresh for review
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.auto_merge != 'true'
        run: >-
          echo "::warning::BiliBili rules refresh requires review:
          ${{ steps.review_pr.outputs.url }}"
