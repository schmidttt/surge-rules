name: Sync GoogleCN rules

on:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * *"
      timezone: "Asia/Shanghai"
    - cron: "0 4 * * 2,4,6"
      timezone: "Asia/Shanghai"

concurrency:
  group: googlecn-rules-sync
  cancel-in-progress: false

jobs:
  build-and-open-review-pr:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (vars.ENABLE_SCHEDULED_SYNC == 'true' &&
        ((github.event.schedule == '0 4 * * *' && vars.SYNC_PHASE != 'stable') ||
         (github.event.schedule == '0 4 * * 2,4,6' && vars.SYNC_PHASE == 'stable')))
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Check out default branch
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Run offline tests
        run: |
          python3 -m unittest discover -s tests/shared -v
          python3 -m unittest discover -s tests/googlecn -v

      - name: Resolve and pin current upstream commit
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: python3 scripts/googlecn/build_googlecn_rules.py --fetch

      - name: Normalize risk classification
        run: >-
          python3 scripts/shared/risk_policy.py
          --assessment reports/googlecn/change-assessment.json

      - name: Validate repository
        run: python3 scripts/validate_repository.py

      - name: Show generated changes
        run: git diff --check && git diff --stat

      - name: Detect changes
        id: changes
        shell: bash
        run: |
          if git diff --quiet -- rules/GoogleCN reports/googlecn; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Read risk assessment
        if: steps.changes.outputs.changed == 'true'
        id: risk
        shell: bash
        run: |
          echo "auto_merge=$(jq -r '.auto_merge_eligible' reports/googlecn/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "classification=$(jq -r '.classification' reports/googlecn/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "added=$(jq -r '.added_count' reports/googlecn/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "removed=$(jq -r '.removed_count' reports/googlecn/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "new_review=$(jq -r '.new_unresolved_candidate_count' reports/googlecn/change-assessment.json)" >> "$GITHUB_OUTPUT"

      - name: Create or update review branch
        if: steps.changes.outputs.changed == 'true'
        id: review_pr
        env:
          GH_TOKEN: ${{ github.token }}
          BASE_BRANCH: ${{ github.event.repository.default_branch }}
          REVIEW_BRANCH: automation/googlecn-rules-sync
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin "$REVIEW_BRANCH:refs/remotes/origin/$REVIEW_BRANCH" || true
          git switch -C "$REVIEW_BRANCH"
          git add rules/GoogleCN reports/googlecn
          git commit -m "chore: sync GoogleCN rules"
          git push --force-with-lease origin "HEAD:$REVIEW_BRANCH"

          REASONS=$(jq -r 'if (.reasons | length) == 0 then "none" else (.reasons | join(", ")) end' reports/googlecn/change-assessment.json)
          cat > /tmp/googlecn-rules-pr.md <<EOF
          Automated conservative GoogleCN refresh.

          - Classification: ${{ steps.risk.outputs.classification }}
          - Added rules: ${{ steps.risk.outputs.added }}
          - Removed rules: ${{ steps.risk.outputs.removed }}
          - New unresolved candidates: ${{ steps.risk.outputs.new_review }}
          - Review reasons: ${REASONS}

          Ambiguous candidates stay out of GoogleCN.list.
          Review report: reports/googlecn/review.md
          Review guide: docs/rules/googlecn/REVIEW_CHECKLIST.md
          EOF

          if PR_URL=$(gh pr view "$REVIEW_BRANCH" --json url --jq .url 2>/dev/null); then
            gh pr edit "$PR_URL" --body-file /tmp/googlecn-rules-pr.md
          else
            PR_URL=$(gh pr create \
              --base "$BASE_BRANCH" \
              --head "$REVIEW_BRANCH" \
              --title "chore: review GoogleCN rules sync" \
              --body-file /tmp/googlecn-rules-pr.md)
          fi
          echo "url=$PR_URL" >> "$GITHUB_OUTPUT"

      - name: Notify repository owner for risky refresh
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.classification != 'low-risk'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 scripts/shared/notify_review.py \
            --repository "${{ github.repository }}" \
            --pr-url "${{ steps.review_pr.outputs.url }}" \
            --reviewer "${{ github.repository_owner }}" \
            --product "GoogleCN" \
            --assessment reports/googlecn/change-assessment.json \
            --evidence reports/googlecn/googlecn-report.json \
            --review-guide docs/rules/googlecn/REVIEW_CHECKLIST.md

      - name: Auto-merge low-risk refresh
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.auto_merge == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 scripts/shared/merge_low_risk.py \
            --repository "${{ github.repository }}" \
            --pr-url "${{ steps.review_pr.outputs.url }}" \
            --assessment reports/googlecn/change-assessment.json \
            --workflow validate-repository.yml

      - name: Keep refresh for review
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.auto_merge != 'true'
        run: >-
          echo "::warning::GoogleCN rules refresh requires review:
          ${{ steps.review_pr.outputs.url }}"
