name: Sync Game rules

on:
  workflow_dispatch:
  schedule:
    - cron: "5 4 * * *"
      timezone: "Asia/Shanghai"
    - cron: "5 4 * * 2,4,6"
      timezone: "Asia/Shanghai"

concurrency:
  group: game-rules-sync
  cancel-in-progress: false

jobs:
  build-and-open-review-pr:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (vars.ENABLE_SCHEDULED_SYNC == 'true' &&
        ((github.event.schedule == '5 4 * * *' && vars.SYNC_PHASE != 'stable') ||
         (github.event.schedule == '5 4 * * 2,4,6' && vars.SYNC_PHASE == 'stable')))
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Check out default branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.12"

      - name: Run offline tests
        run: |
          python3 -m unittest discover -s tests/shared -v
          python3 -m unittest discover -s tests/game -v

      - name: Resolve and pin current upstream commit
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: python3 scripts/game/build_game_rules.py --fetch

      - name: Normalize risk classification
        run: >-
          python3 scripts/shared/risk_policy.py
          --assessment reports/game/change-assessment.json

      - name: Validate repository
        run: python3 scripts/validate_repository.py

      - name: Show generated changes
        run: git diff --check && git diff --stat

      - name: Detect changes
        id: changes
        shell: bash
        run: |
          if git diff --quiet -- rules/Game rules/GameCN reports/game; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Read risk assessment
        if: steps.changes.outputs.changed == 'true'
        id: risk
        shell: bash
        run: |
          echo "auto_merge=$(jq -r '.auto_merge_eligible' reports/game/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "classification=$(jq -r '.classification' reports/game/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "added=$(jq -r '.added_count' reports/game/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "removed=$(jq -r '.removed_count' reports/game/change-assessment.json)" >> "$GITHUB_OUTPUT"
          echo "ratio=$(jq -r '.actual_change_ratio' reports/game/change-assessment.json)" >> "$GITHUB_OUTPUT"

      - name: Create or update review branch
        if: steps.changes.outputs.changed == 'true'
        id: review_pr
        env:
          GH_TOKEN: ${{ github.token }}
          BASE_BRANCH: ${{ github.event.repository.default_branch }}
          REVIEW_BRANCH: automation/game-rules-sync
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin "$REVIEW_BRANCH:refs/remotes/origin/$REVIEW_BRANCH" || true
          git switch -C "$REVIEW_BRANCH"
          git add rules/Game rules/GameCN reports/game
          git commit -m "chore: sync Game rules"
          git push --force-with-lease origin "HEAD:$REVIEW_BRANCH"

          REASONS=$(jq -r 'if (.reasons | length) == 0 then "none" else (.reasons | join(", ")) end' reports/game/change-assessment.json)
          cat > /tmp/game-rules-pr.md <<EOF
          Automated Game and GameCN refresh.

          - Classification: ${{ steps.risk.outputs.classification }}
          - Added rules: ${{ steps.risk.outputs.added }}
          - Removed rules: ${{ steps.risk.outputs.removed }}
          - Actual change ratio: ${{ steps.risk.outputs.ratio }}
          - Review reasons: ${REASONS}

          v2fly is the only formal source. GameCN must precede Game in Surge.
          Review report: reports/game/review.md
          Review guide: docs/rules/game/REVIEW_CHECKLIST.md
          EOF

          PR_URL=$(gh pr list \
            --base "$BASE_BRANCH" \
            --head "$REVIEW_BRANCH" \
            --state open \
            --json url \
            --jq '.[0].url // ""')

          if [ -n "$PR_URL" ]; then
            gh pr edit "$PR_URL" --body-file /tmp/game-rules-pr.md
          else
            PR_URL=$(gh pr create \
              --base "$BASE_BRANCH" \
              --head "$REVIEW_BRANCH" \
              --title "chore: review Game rules sync" \
              --body-file /tmp/game-rules-pr.md)
          fi
          echo "url=$PR_URL" >> "$GITHUB_OUTPUT"
          echo "head_sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Notify repository owner for risky refresh
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.classification != 'low-risk'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 scripts/shared/notify_review.py \
            --repository "${{ github.repository }}" \
            --pr-url "${{ steps.review_pr.outputs.url }}" \
            --reviewer "${{ github.repository_owner }}" \
            --product "Game / GameCN" \
            --assessment reports/game/change-assessment.json \
            --evidence reports/game/game-report.json \
            --review-guide docs/rules/game/REVIEW_CHECKLIST.md

      - name: Auto-merge low-risk refresh
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.auto_merge == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 scripts/shared/merge_low_risk.py \
            --repository "${{ github.repository }}" \
            --pr-url "${{ steps.review_pr.outputs.url }}" \
            --assessment reports/game/change-assessment.json \
            --workflow validate-repository.yml \
            --allowed-path rules/Game \
            --allowed-path rules/GameCN \
            --allowed-path reports/game \
            --expected-branch automation/game-rules-sync \
            --expected-head-sha "${{ steps.review_pr.outputs.head_sha }}"

      - name: Keep refresh for review
        if: >-
          steps.changes.outputs.changed == 'true' &&
          steps.risk.outputs.auto_merge != 'true'
        run: >-
          echo "::warning::Game rules refresh requires review:
          ${{ steps.review_pr.outputs.url }}"
